# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Realm-Backup-Sample' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Realm-Backup-Sample
  pod 'RealmSwift'

  target 'Realm-Backup-SampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Realm-Backup-SampleUITests' do
    # Pods for testing
  end

end
